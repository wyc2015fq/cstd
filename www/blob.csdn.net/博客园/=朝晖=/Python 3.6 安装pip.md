# Python 3.6 安装pip - =朝晖= - 博客园
# [Python 3.6 安装pip](https://www.cnblogs.com/dhcn/p/10280731.html)
     1、首先python3.4以后，pip内嵌，不过使用方法是：
```
python -m pip install django
```
      2、如果你觉得上面的指令太长，想用旧方法使用pip，那用以下指令安装pip即可
```
python -m pip install -U pip
```

