# vs添加宏，_用户5706034127_新浪博客
|||
你在 头文件.h里面的宏，和下面的宏是没有区别的
**VS中添加预处理宏的方法**
除了在.c及.h中添加宏定义之外，还可以采用如下方法添加宏定义：
1、若只需要定义一个宏(如#define
DEBUG)，可以右键点击工程-->属性-->c/c++-->预处理器-->预处理器定义，点击下拉框中的编辑，输入想要定义的宏；
2、如果还需要定义宏的内容(如#define inline
__inline)，可以右键点击工程-->属性-->c/c++-->命令行，在其它选项中输入如下内容： /D"inline"=__inline
。
