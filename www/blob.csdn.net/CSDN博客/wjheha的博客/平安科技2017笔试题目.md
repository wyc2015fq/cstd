# 平安科技2017笔试题目 - wjheha的博客 - CSDN博客
2017年09月21日 22:49:10[wjheha](https://me.csdn.net/wjheha)阅读数：8264
个人分类：[数据结构和算法](https://blog.csdn.net/wjheha/article/category/6854909)
题目要求见图片，可放大浏览器查看！ 
![这里写图片描述](https://img-blog.csdn.net/20170921224707759?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQvd2poZWhh/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast)
![这里写图片描述](https://img-blog.csdn.net/20170921224718855?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQvd2poZWhh/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast)
代码见下面，无重复分类递归解决，效率还可以！
```
package others;
import java.util.Scanner;
public class CoinCoinPingAn {
    public static void main(String[] args) {
        // 从命令行读取数据
        Scanner in = new Scanner(System.in);
        String s1 = in.nextLine();
        String[] s2 = s1.split("\\=|,");
        int n = Integer.parseInt(s2[1]); //总的钱数目
        int[] num = new int[6];   //这是每个钱的数目
        for(int i = 3; i < s2.length; i = i + 2) {
            num[i/2 - 1] = Integer.parseInt(s2[i]);
        }
        int times = coinCoinPingAn(n, num);  //总的拼凑次数
        System.out.println(times);
    }
    private static int coinCoinPingAn(int n, int[] num) {
        int[] coinValue = {1, 5, 10, 20, 50, 100};
        int m = coinValue.length - 1;
        int times = coinCoinPingAn(n, coinValue, m, num);
        return times;
    }
    private static int coinCoinPingAn(int n, int[] coinValue, int m, int[] num) {
        if(n == 0){
            return 1;
        }
        if(n < 0) {
            return 0;
        }
        if(m < 0) {
            return 0;
        }
        int times_temp = 0;
        for(int i = 0; i <= num[m]; i++) {
            times_temp = times_temp + coinCoinPingAn(n - i*coinValue[m], coinValue, m -1, num);
        }
        return times_temp;
    }
}
```
运行效果：
```
n=100,a1=0,a2=0,a3=5,a4=6,a5=3,a6=2
8
```
