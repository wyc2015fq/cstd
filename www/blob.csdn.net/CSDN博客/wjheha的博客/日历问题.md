# 日历问题 - wjheha的博客 - CSDN博客
2018年11月23日 16:06:48[wjheha](https://me.csdn.net/wjheha)阅读数：123
问题：给定从公元2000年1月1日开始逝去的天数，请编写程序给出这一天是哪年哪月哪日星期几？
```
#include<iostream>
using namespace std;
int days;
int get_dayofweek();
int get_year();
int get_month(int);
int main() {
//日历问题,给出从2000年1月1日起n天后是多少年月日星期
	int year, month, dayofweek;
	int leap_year;
	char week[7][10] = { "Saturday", "Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday" };  //2000年1月1日是星期6
	while ((cin >> days) && days != -1) {
		dayofweek = get_dayofweek();
		year = get_year();
		leap_year = (year % 4 == 0 && year % 100 != 0 || year % 400 == 0);
		month = get_month(leap_year);
		cout << year << "-" << month << "-" << ++days << " " << week[dayofweek];
	}
	return 0;
} 
//计算星期几
int get_dayofweek() {
	int dayofweek;
	dayofweek = days % 7;
	return dayofweek;
}
//计算年数
int get_year() {
	int i = 2000, leap_year;
	while (1) {
		leap_year = (i % 4 == 0 && i % 100 != 0 || i % 400 == 0);
		if (leap_year == 1 && days >= 366) {
			days = days - 366;
			i++;
			continue;
		}
		else if(leap_year == 0 && days >= 365) {
			days = days - 365;
			i++;
			continue;
		}
		else {
			break;
		}
	}
	return i;
}
//计算月份
int get_month(int leap_year) {
	int pmonth[12] = { 31,28,31,30,31,30,31,31,30,30,31 };
	int rmonth[12] = { 31,29,31,30,31,30,31,31,30,30,31 };
	int j = 0;
	while (1) {
		if (leap_year == 1 && days >= rmonth[j]) {
			days = days - rmonth[j];
			j++;
		}
		else if (leap_year == 0 && days >= pmonth[j]) {
			days = days - pmonth[j];
			j++;
		}
		else {
			break;
		}
	}
	return ++j;
}
```
![在这里插入图片描述](https://img-blog.csdnimg.cn/20181123161516142.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dqaGVoYQ==,size_16,color_FFFFFF,t_70)
逆向问题：已知2012年1月25日是星期三，编写一个程序，输入用“年 月 日”表示的一个2012年1月25日以后的期，输出该日期是星期几(星期天输出0)。
```
#include <iostream>
using namespace std;
int monthDays[13] = {-1,31,28,31,30,31,30,31,31,30,31,30,31};
int main()
{
	int year,month,date;
	int days = 0; //从2012-01-22开始过了多少天
	cin >> year >> month >> date;
	for(int y = 2012; y < year; ++y)
		{
			if( (y%4 ==0 && y%100!= 0) || y%400 == 0)
				days += 366;
			else
				days += 365;
		}
	if( (year%4 ==0 && year%100!= 0) || year%400 == 0)
		monthDays[2] = 29;
	for(int m = 1; m < month; ++m)
		days += monthDays[m];
	days += date;
	days -= 22;
	cout << days % 7 << endl;
	return 0;
}
```
![在这里插入图片描述](https://img-blog.csdnimg.cn/20181213214921713.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dqaGVoYQ==,size_16,color_FFFFFF,t_70)
