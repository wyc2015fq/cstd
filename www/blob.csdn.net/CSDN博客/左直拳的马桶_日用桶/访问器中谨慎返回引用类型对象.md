# 访问器中谨慎返回引用类型对象 - 左直拳的马桶_日用桶 - CSDN博客
2014年09月04日 23:14:59[左直拳](https://me.csdn.net/leftfist)阅读数：1491
个人分类：[Java](https://blog.csdn.net/leftfist/article/category/2412493)
面向对象三大特性之一是封装，类中的域（变量），尽量使用private，如果真要访问，可以提供get和set，以避免破坏封装性。
但是，如果get访问器中返回的是引用类型对象的话，那也有可能会被外界所修改，从而破坏封装性。如：
```java
public class Employee {
	private Date hireDay;
	public Date getHireDay(){
		return this.hireDay;
	}
	
	private String name;
	public Employee(String name){
		this.name = name;
	}
	public boolean equals(Employee other){
		return name.equalsIgnoreCase(other.name);
	}
}
```
```java
Employee zhang3 = new Employee("zhang3");
		Date d = zhang3.getHireDay();
		d.setTime(d.getTime() - 10000000);
```
这一点，是我以前所没想到的。            
