# 方法或函数也可以用泛型 - 左直拳的马桶_日用桶 - CSDN博客
2011年12月02日 18:31:29[左直拳](https://me.csdn.net/leftfist)阅读数：1143标签：[class																[null](https://so.csdn.net/so/search/s.do?q=null&t=blog)](https://so.csdn.net/so/search/s.do?q=class&t=blog)
个人分类：[.NET](https://blog.csdn.net/leftfist/article/category/94497)
原来泛型不单只对类的，也可以只使用在方法或函数里面：
```
public abstract class BaseClass
    {
        protected static T MyGetModel<T>(DataTable dt) where T : class,new()
        {
            DataRow dr = dt.Rows[0];
            T model = new T();
            PropertyInfo[] pis = model.GetType().GetProperties();
            foreach (PropertyInfo pi in pis)
            {
                try
                {
                    if (dt.Columns.Contains(pi.Name) && dr[pi.Name].ToString() != "")
                    {
                        pi.SetValue(model, dr[pi.Name] ?? "", null);
                    }
                }
                catch { }
            }
            return model;
        }
    }
```
