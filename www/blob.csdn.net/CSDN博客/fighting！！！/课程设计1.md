# 课程设计1 - fighting！！！ - CSDN博客
2015年05月22日 19:51:34[dujiahei](https://me.csdn.net/dujiahei)阅读数：294
个人分类：[汇编语言学习](https://blog.csdn.net/dujiahei/article/category/3259223)
assume cs:code
data segment
db '1975','1976','1977','1978','1979','1980','1981','1982','1983'
db '1984','1985','1986','1987','1988','1989','1990','1991','1992'
db '1993','1994','1995'
;以上是表示21年的21个字符串 4 * 21 = 84
dd 16,22,382,1356,2390,8000,16000,24486,50065,97479,140417,197514
dd 345980,590827,803530,1183000,1843000,2759000,3753000,4649000,5937000
;以上是表示21年公司总收入的21个dword型数据 4 * 21 = 84
dw 3,7,9,13,28,38,130,220,476,778,1001,1442,2258,2793,4037,5635,8226
dw 11542,14430,15257,17800
;以上是表示21年公司雇员人数的21个word型数据  2 * 21 = 42
data ends ;84+84+42=210 个字节 占用 14行
string segment
dw 21 dup (0)
string ends
table segment
dw 21 dup(0)
table ends
code segment
start: mov ax,data 
   mov es,ax
        mov di,0
        mov ax,string
 mov ds,ax
        mov si,0
 mov dh,1
 push di
 push dx       
        mov cx,21        
 showlp:
 push cx
;year   先传至string段，再传显存
        mov ax,es:[di] 
 mov ds:[si],ax
        mov ax,es:[di+2] 
 mov ds:[si+2],ax
        mov byte ptr ds:[si+4],0
        mov dl,3
 mov cl,2
 call show_str
;income  双字转字符串string段，传至显存
 push dx
 push ax
 mov ax,es:84[di] ;数组   
 mov dx,es:86[di]
        ;ds:si   
 call dwtoc
 pop ax
 pop dx
 mov dl,23
 call show_str
  add di,4
 inc dh
 pop cx
  loop showlp
 pop dx
 pop di
        mov cx,21    
 showlp2: push cx  ;4d
;employee
 push dx
 mov ax,es:168[di] 
 mov dx,0
 mov ds:[si],ax      
 call dwtoc
 pop dx  
 mov dl,43
 mov cl,2
 call show_str
;average 先计算出来，存入string段，再转字符串，传显存
 push dx
 push bx
 mov ax,es:84[bx]
 mov dx,es:86[bx]
 mov cx,es:168[di] 
 call divdw
 mov ds:[si],ax
 mov dx,0
 call dwtoc
 pop bx
 pop dx
 mov dl,63
 mov cl,2
 call show_str
 add di,2
 add bx,4
 inc dh
        pop cx
  loop showlp2
 mov ax,4c00h
 int 21h
show_str:push cx
 push dx 
 push es
 push di
 push bx
 push si 
 push ax 
 mov ax,0b800h
 mov es,ax
        mov al,dl
        dec al
        mov ah,0
        add ax,ax
        mov di,ax
 mov dl,dh  
 mov al,160
 mul dl
 add di,ax 
       mov bh,cl 
show: 
 mov cl,ds:[si]
 mov ch,0
        jcxz ok
 mov bl,ds:[si]
        mov es:[di],bx
        inc si
        add di,2
 jmp show
ok: pop ax
 pop si
 pop bx
 pop di
 pop es
 pop dx
 pop cx
 ret
dwtoc: push bx 
 push cx
        push dx
        push si
        push di
        mov di,0
s1:     mov cx,10
 call divdw
        add cx,30h
        push cx
 inc di
        mov cx,ax
 jcxz ok1
 jmp s1 
ok1:           
      mov cx,di
    s2: pop ds:[si]
 inc si
        loop s2
 mov byte ptr ds:[si],0
 pop di
        pop si
        pop dx
 pop cx
 pop bx
 ret
divdw: push bx ;为了子程序的通用性而入栈
 push ax  ;为了子程序后面调用
 mov ax,dx
 mov dx,0
 div cx 
 mov bx,ax        
 pop ax 
 div cx
 mov cx,dx
 mov dx,bx 
        pop bx  
 ret
code ends
end start
