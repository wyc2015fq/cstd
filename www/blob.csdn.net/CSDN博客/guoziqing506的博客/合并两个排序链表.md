# 合并两个排序链表 - guoziqing506的博客 - CSDN博客





2016年04月29日 15:12:04[guoziqing506](https://me.csdn.net/guoziqing506)阅读数：741标签：[链表](https://so.csdn.net/so/search/s.do?q=链表&t=blog)
个人分类：[lintcode](https://blog.csdn.net/guoziqing506/article/category/6289140)

所属专栏：[LintCode-Python题解](https://blog.csdn.net/column/details/guoziqing-blog.html)









题目描述：将两个排序链表合并为一个新的排序链表

样例：给出 1->3->8->11->15->null，2->null， 返回 1->2->3->8->11->15->null。

之前学过合并两个排序数组（详见：[点击打开链接](http://blog.csdn.net/guoziqing506/article/details/50949830)），基本的思路是用两个指针分别指向两个数组中的元素，按照所指元素的大小关系，依次排列。具体的原理如果不明白，请回看我刚才给的链接。那么合并两个链表呢？从本质上讲没什么不同，以其中一个链表为基本，将另一个链表的元素按大小关系依次插入到这个基本链表即可，当然插入的过程要特别小心，“摘链”和“链接”时小心对节点的错误引用。

因为原理是简单的，所以直接看代码吧



```python
"""
Definition of ListNode
class ListNode(object):
    def __init__(self, val, next=None):
        self.val = val
        self.next = next
"""
class Solution:
    """
    @param two ListNodes
    @return a ListNode
    """
    def mergeTwoLists(self, l1, l2):
        p1, p2 = l1, l2
        dummy = ListNode(-1)
        dummy.next = l1
        pre = dummy
        while p1 and p2:
            if p1.val > p2.val:
                temp = p2
                p2 = p2.next
                pre.next = temp
                temp.next = p1
                pre = pre.next
            else:
                pre = pre.next
                p1 = p1.next
        if p2:
            pre.next = p2
        return dummy.next
                
        # write your code here
```
需要注意的是节点的引用问题，我们用的head,pre,cur,p1,p2这些节点的名字其实都是对节点所在位置的引用，拿我之前长举的例子来说，就是在“房子”（节点）上挂的“门牌”（引用），一个门上当然可以同时挂两个或者多个门牌，而他们代表的都是这个“房间”（节点）。根据这个“房间理论”，再理解一下19-24行，一定要亲自动手写一遍。






