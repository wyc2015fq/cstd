# 【算法题】字符移位 - xiaxzhou的博客 - CSDN博客





2017年05月31日 09:32:11[xiaxzhou](https://me.csdn.net/xiaxzhou)阅读数：150








> 
小Q最近遇到了一个难题：把一个字符串的大写字母放到字符串的后面，各个字符的相对位置不变，且不能申请额外的空间。 

  你能帮帮小Q吗？

输入描述: 

  输入数据有**多组**，每组包含一个字符串s，且保证:1<=s.length<=1000.

输出描述: 

  对于**每组**数据，输出移位后的字符串。

输入例子: 

  AkleBiCeilD

输出例子: 

  kleieilABCD

注意输入输出的格式：循环输入，输出换行  

开始没写endl一直不通过

**endl:**    输出时插入换行符并刷新流 

 没有这个endl输出一直没出缓冲区，直到缓冲区满了才刷新出来，endl的作用可以将缓冲区刷新出来。

```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main()
{
    vector<string> vec;
    string str;
    while (cin >> str)
    {
        int k = str.size();
        int count(0);
        for (int i = 0; i < k; ++i)
        {
            if (str[i] >= 'A'&&str[i] <= 'Z')
            {
                count++;
            }
            else
            {
                int index = i;
                for (auto j = 0; j < count; j++)
                {
                    std::swap(str[index], str[index - 1]);
                    index--;
                }
            }
        }
        cout << str<<endl;//开始没写endl老是通不过
    }

    return 0;
}
```

有大神给出**神解法**：

```cpp
链接：https://www.nowcoder.com/questionTerminal/7e8aa3f9873046d08899e0b44dac5e43
来源：牛客网

#include <iostream>
#include <string.h>
using namespace std;
int main(){
    string s;
    while(cin >> s){
        if(s.length() >= 1 && s.length() <= 1000){
            for(int i = 0; i < s.length(); i++)
                if(s[i] >= 'a' && s[i] <= 'z')
                    cout << s[i];
            for(int i = 0; i < s.length(); i++)
                if(s[i] <= 'Z' && s[i] >= 'A')
                    cout << s[i];
            cout << endl;
        }
    }
    return 0;
}
```

这个空子钻得，腾讯员工都要报警了




