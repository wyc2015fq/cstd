# hdu2031——进制转换 - westbrook1998的博客 - CSDN博客





2018年02月19日 19:13:46[westbrook1998](https://me.csdn.net/westbrook1998)阅读数：92








## 题目：

> 
Problem Description 

  输入一个十进制数N，将它转换成R进制数输出。  

Input 

  输入数据包含多个测试实例，每个测试实例包含两个整数N(32位整数)和R（2<=R<=16, R<>10）。  

Output 

  为每个测试实例输出转换后的数，每个输出占一行。如果R大于10，则对应的数字规则参考16进制（比如，10用A表示，等等）。  

Sample Input 

  7 2 

  23 12 

  -4 3  
Sample Output 

  111 

  1B 

  -11  
Author 

  lcy   

## 题解：

```cpp
#include <stdio.h>
#include <string.h>

char num[1001];
int n,r,i,j;
int main(void)
{
    while(scanf("%d %d",&n,&r)!=EOF)
    {
        if(0==n)
        {
            printf("0\n");
            continue;
        }
        memset(num,'\0',sizeof(num));
        i=0;
        if(n<0)
        {
            n*=-1;
            printf("-");
        }
        while(n>0)
        {
            if(n%r>=10)
                num[i++]=n%r-10+'A';
            else
                num[i++]=n%r+'0';
            n/=r;
        } 
        for(j=i-1;j>=0;j--)
        {
            printf("%c",num[j]);
        }
        printf("\n");
    }
    return 0;
}
```






