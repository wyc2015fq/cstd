# 175. 翻转二叉树 - westbrook1998的博客 - CSDN博客





2018年04月20日 21:03:43[westbrook1998](https://me.csdn.net/westbrook1998)阅读数：38标签：[算法																[二叉树																[翻转二叉树](https://so.csdn.net/so/search/s.do?q=翻转二叉树&t=blog)
个人分类：[Lintcode](https://blog.csdn.net/westbrook1998/article/category/7459920)





翻转二叉树 

递归方法：

```java
public class Solution {
    /**
     * @param root: a TreeNode, the root of the binary tree
     * @return: nothing
     */
    public void invertBinaryTree(TreeNode root) {
        // write your code here
        if(root==null){
            return;
        }
        else{
            TreeNode t=root.left;
            root.left=root.right;
            root.right=t;
            invertBinaryTree(root.left);
            invertBinaryTree(root.right);
        }
    }
}
```

非递归方法：（用栈来代替递归）

```java
public class Solution {
    /**
     * @param root: a TreeNode, the root of the binary tree
     * @return: nothing
     */
    public void invertBinaryTree(TreeNode root) {
        // write your code here
        if(root==null){
            return;
        }
        else{
            Stack<TreeNode> s=new Stack<>();
            s.push(root);
            while(!s.empty()){
                TreeNode t=s.pop();
                exc(t);
                if(t.left!=null){
                    s.push(t.left);
                }
                if(t.right!=null){
                    s.push(t.right);
                }
            }
        }
    }
    private void exc(TreeNode s){
        TreeNode t=s.left;
        s.left=s.right;
        s.right=t;
    }
}
```](https://so.csdn.net/so/search/s.do?q=二叉树&t=blog)](https://so.csdn.net/so/search/s.do?q=算法&t=blog)




