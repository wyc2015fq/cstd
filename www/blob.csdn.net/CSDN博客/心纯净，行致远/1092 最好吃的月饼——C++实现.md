# 1092 最好吃的月饼——C++实现 - 心纯净，行致远 - CSDN博客





2018年12月10日 11:05:18[吉大秦少游](https://me.csdn.net/zhanshen112)阅读数：257








# 题目

> 
最好吃的月饼 （20 分）

月饼是久负盛名的中国传统糕点之一，自唐朝以来，已经发展出几百品种。

![mk.jpg](https://images.ptausercontent.com/fcb325a0-7090-4bf4-acb0-d4d7ea832f27.jpg)

若想评比出一种“最好吃”的月饼，那势必在吃货界引发一场腥风血雨…… 在这里我们用数字说话，给出全国各地各种月饼的销量，要求你从中找出销量冠军，认定为最好吃的月饼。

### 输入格式：

输入首先给出两个正整数 N（≤1000）和 M（≤100），分别为月饼的种类数（于是默认月饼种类从 1 到 N 编号）和参与统计的城市数量。

接下来 M 行，每行给出 N 个非负整数（均不超过 1 百万），其中第 i 个整数为第 i 种月饼的销量（块）。数字间以空格分隔。

### 输出格式：

在第一行中输出最大销量，第二行输出销量最大的月饼的种类编号。如果冠军不唯一，则按编号递增顺序输出并列冠军。数字间以 1 个空格分隔，行首尾不得有多余空格。

### 输入样例：

```
5 3
1001 992 0 233 6
8 0 2018 0 2008
36 18 0 1024 4
```

### 输出样例：

```
2018
3 5
```


# 算法

思路很简单，将对应序号的月饼加起来存入sum[N+1]，然后sort排序。排序之前保留一个sum的副本，好记录月饼序号。代码1是考试时写的，实际上用不到结构体，数组就行。题目主要考察的还是sort函数。代码2是不用结构体重写的代码。

# 代码

1、

```cpp
#include <iostream>
#include <algorithm>
using namespace std;
struct node{
	int id;
	int sale;
};
int main(){
	int N,M;	cin>>N>>M;
	node mooncake[N];
	int sum[N]={0},idx[N];
	for(int i=0;i<M;i++){
		getchar();
		for(int j=0;j<N;j++){
			mooncake[j].id =j+1;
			cin>>mooncake[i].sale ;
			sum[j]=sum[j]+mooncake[i].sale;
			idx[j]=sum[j];
		}
	}
	sort(sum,sum+N);	cout<<sum[N-1]<<endl;
	int flag=0;
	for(int i=0;i<N;i++){
		if(idx[i]==sum[N-1]){
			if(flag==1)	cout<<" ";
			cout<<i+1;
			flag=1;
		}
	}	
	return 0;
}
```

2、

```cpp
#include <iostream>
#include <algorithm>
using namespace std;
int main(){
	int N,M,flag=0;	cin>>N>>M;
	int sum[N+1]={0},idx[N+1]={0};
	for(int i=0;i<M;i++){
		getchar();
		for(int j=1;j<=N;j++){
			int tmp;	cin>>tmp;
			sum[j]=sum[j]+tmp;
			idx[j]=sum[j];
		}
	}
	sort(sum,sum+N+1);	cout<<sum[N]<<endl;
	for(int i=1;i<=N;i++){
		if(idx[i]==sum[N]){
			if(flag==1)	cout<<" ";
			cout<<i;
			flag=1;
		}
	}	
	return 0;
}
```





