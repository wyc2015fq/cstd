# 1033 旧键盘打字——C/C++实现 - 心纯净，行致远 - CSDN博客





2018年11月25日 21:32:39[吉大秦少游](https://me.csdn.net/zhanshen112)阅读数：46标签：[C																[C  																[PAT](https://so.csdn.net/so/search/s.do?q=PAT&t=blog)
个人分类：[【PAT_BasicLevel】](https://blog.csdn.net/zhanshen112/article/category/8241634)





# 题目

> 
1033 旧键盘打字 （20 point(s)）

旧键盘上坏了几个键，于是在敲一段文字的时候，对应的字符就不会出现。现在给出应该输入的一段文字、以及坏掉的那些键，打出的结果文字会是怎样？

### 输入格式：

输入在 2 行中分别给出坏掉的那些键、以及应该输入的文字。其中对应英文字母的坏键以大写给出；每段文字是不超过 10​5​​ 个字符的串。可用的字符包括字母 [`a`-`z`, `A`-`Z`]、数字 `0`-`9`、以及下划线 `_`（代表空格）、`,`、`.`、`-`、`+`（代表上档键）。题目保证第 2 行输入的文字串非空。

注意：如果上档键坏掉了，那么大写的英文字母无法被打出。

### 输出格式：

在一行中输出能够被打出的结果文字。如果没有一个字符能被打出，则输出空行。

### 输入样例：

```
7+IE.
7_This_is_a_test.
```

### 输出样例：

```
_hs_s_a_tst
```


# 算法

先看代码2，这个比较容易理解。将坏键记录下来，并在输入时先判断大写有没有坏，再分别进行判断。如果上档键坏了，所有大写字母不输出，所有被记录的坏键也不输出，其他都输出；如果上档键没坏，只有记录的坏键不输出，其他都输出。

代码1是代码的精简凝练。

# 代码

1、C++

```cpp
//PAT1033V1
#include <stdio.h>
#include <ctype.h>
int main()
{
    char c;
    int bad[128] = {0}; /* record keys are broken or not */
    while((c = getchar()) != '\n')      /* read broken keys */
        bad[toupper(c)] = 1;
    while((c = getchar()) != '\n')      /* read string and print */
        if(!bad[toupper(c)] && !(isupper(c) && bad['+']))
          putchar(c);
    return 0;
}
```

2、C++

```cpp
#include <iostream>
#include <cctype>
using namespace std;
int main(){
	string s1,s2;
	char c;	
	int b[128]={0};
	while((c=getchar())!='\n')	b[toupper(c)]=1;
	while((c=getchar())!='\n'){
		if(b['+']==1){	//大写字母坏了 
			if(isupper(c));	//c是大写 
			else if(b[toupper(c)]==1);	//c是小写 
			else	putchar(c);
		}
		else{	//大写字母没坏 
			if(b[toupper(c)]==1);	//c是大写 
			else putchar(c);
		}
	}
	return 0;
}
```](https://so.csdn.net/so/search/s.do?q=C  &t=blog)](https://so.csdn.net/so/search/s.do?q=C&t=blog)




