# 1014 福尔摩斯的约会——C/C++实现 - 心纯净，行致远 - CSDN博客





2018年11月21日 22:22:46[吉大秦少游](https://me.csdn.net/zhanshen112)阅读数：90








# 题目

> 
1014 福尔摩斯的约会 （20 point(s)）

大侦探福尔摩斯接到一张奇怪的字条：`我们约会吧！ 3485djDkxh4hhGE 2984akDfkkkkggEdsb s&hgsfdk d&Hyscvnm`。大侦探很快就明白了，字条上奇怪的乱码实际上就是约会的时间`星期四 14:04`，因为前面两字符串中第 1 对相同的大写英文字母（大小写有区分）是第 4 个字母 `D`，代表星期四；第 2 对相同的字符是 `E` ，那是第 5 个英文字母，代表一天里的第 14 个钟头（于是一天的 0 点到 23 点由数字 0 到 9、以及大写字母 `A` 到 `N` 表示）；后面两字符串第 1 对相同的英文字母 `s` 出现在第 4 个位置（从 0 开始计数）上，代表第 4 分钟。现给定两对字符串，请帮助福尔摩斯解码得到约会的时间。

### 输入格式：

输入在 4 行中分别给出 4 个非空、不包含空格、且长度不超过 60 的字符串。

### 输出格式：

在一行中输出约会的时间，格式为 `DAY HH:MM`，其中 `DAY` 是某星期的 3 字符缩写，即 `MON` 表示星期一，`TUE` 表示星期二，`WED` 表示星期三，`THU` 表示星期四，`FRI` 表示星期五，`SAT` 表示星期六，`SUN` 表示星期日。题目输入保证每个测试存在唯一解。

### 输入样例：

```
3485djDkxh4hhGE 
2984akDfkkkkggEdsb 
s&hgsfdk 
d&Hyscvnm
```

### 输出样例：

```
THU 14:04
```


# 算法

这道题的难点之一在于读懂题意，难点之二在于对于字符串和字符的处理。这类题目提交时有时候不能完全AC,有个别测试案例不能通过。而且由于不知道测试案例是什么样的，也很难调试。

总结之后发现对于边界条件的测试是一个好方法，但是也只是在某些时候，并不能一次解决所有这样的情况。

好在现在做时，可以仅凭自己的力量完全做出来，知道如何使用c++语法中的字符串，也能表达自己所要表达的。这次写完的C++代码只有33行，而一个月之前写完的C代码（还是参考了别人的代码）有62行，还是进步不小的，加油！！！



2018.12.02 这道题目有点蛋疼，有的地方题目没有叙述清楚，这是最令人讨厌的……在确定周几之后，确定具体钟点的时候，只说是相同的字符，也没说大小写。还要根据后面的A~N去猜。。。

有这样几个需要注意的点：

1、输出周几之后，**i要自加1**，否则判断周几的字母也会用来判断是哪个钟头，会输出13:04；

2、判断周几必须是s1[i]-'A'>=0&&s1[i]-'A'<=6，我之前写的是s1[i]-'A'>=0&&s1[i]-'A'<=25，一直提示段错误segmentation fault，也就是数组越界了

3、判断是否是字母可以用下面的表示

```cpp
(s3[i]-'A'>=0&&s3[i]-'A'<=25)||(s3[i]-'a'>=0&&s3[i]-'a'<=25)
```

但是不太好，尽量使用

```cpp
isalpha(s3[i])
```

该函数头文件在<cctype>中。

# 代码

C++

```cpp
#include <iostream>
#include <string>
using namespace std;

int main(){
	string s1,s2,s3,s4;	cin>>s1>>s2>>s3>>s4;
	int f1=0;
	string week[7]={"MON","TUE","WED","THU","FRI","SAT","SUN"};
	for(int i=0;i<s1.length() ;i++){
		if(s1[i]==s2[i]&&(s1[i]-'A'>=0&&s1[i]-'A'<=6)&&f1==0){	//s1,s2字符串中相等的字符且是大写字母 
			cout<<week[s1[i]-65]<<" ";
			f1=1;
			i++;
		}
		if(s1[i]==s2[i]&&f1==1){	//钟点 
			if(s1[i]-'0'>=0&&s1[i]-'0'<=9){
				printf("%02d:",s1[i]-'0');
				break;
			}
			if(s1[i]-'A'>=0&&s1[i]-'A'<=13){
				printf("%02d:",s1[i]-'A'+10);
				break;
			}
		}	
	}
	for(int i=0;i<s3.length() ;i++){	//分钟 
		if(s3[i]==s4[i]&&isalpha(s3[i])){
			printf("%02d",i);
			break;
		}	
	}
	return 0;
}
```

C

```cpp
//PAT1013V1
#include <stdio.h>
#include <math.h>

int main(){
	int m,n,i,j,k=0;
	scanf("%d %d",&m,&n);
	int a[10000]={0};                                                                                                         
	for(i=2;i<10000;i++){//PAT1014V1
#include <stdio.h>
#include <string.h>

int main(){
	char a[60],b[60],c[60],d[60];
	char Date[7][10]={"MON","TUE","WED","THU","FRI","SAT","SUN"};
	char t[2];
	int i=0,j=0,minute;
	scanf("%s%s%s%s",a,b,c,d);
	while(i<strlen(a)&&i<strlen(b)){
		if(a[i]==b[i]&&a[i]>='A'&&a[i]<='G'){
			t[0]=a[i];
			break;
		}
		i++;
	}
	printf("%s ",Date[a[i]-65]);
	i=i+1;
	while(i<strlen(a)&&i<strlen(b)){
		if(a[i]==b[i]&&((a[i]>='A'&&a[i]<='N')||(a[i]>='0'&&a[i]<='9'))){
			t[1]=a[i];
			break;
		} 
		i++;
	}
	while(j<strlen(c)&&j<strlen(d)){
		if(c[j]==d[j]&&((c[j]>='A'&&c[j]<='Z')||(c[j]>='a'&&c[j]<='z'))){
			minute=j;
			break;
		}
		j++;
	}
	int hour=(t[1]>='0'&&t[1]<='9')?t[1]-'0':t[1]-'A'+10;
	printf("%02d:%02d",hour,minute);
	return 0;
}
		for(j=2;j<=sqrt(i);j++){
			if(i%j==0)	//not prime
			break;
		}
		if(j>sqrt(i)){
			//prime
			a[k++]=i;    
		//	printf("%d",i);
		}	
	}
	for(i=m-1;i<n;i++){
		printf("%d%c",a[i],((i-m+2)%10==0)||(i==n-1)?'\n':' ');
		//if(j=0;j<10;j++)
				
	}
	return 0;
}
```





