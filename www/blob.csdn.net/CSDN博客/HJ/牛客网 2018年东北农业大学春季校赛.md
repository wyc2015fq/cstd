# 牛客网  2018年东北农业大学春季校赛 - HJ - CSDN博客
2018年04月07日 11:34:24[FZH_SYU](https://me.csdn.net/feizaoSYUACM)阅读数：146
A题 wyh的曲线 
待补…
B题 wyh的矩阵 
规律题
```cpp
#include<iostream>
using namespace std;
int main()
{
    long long n,t,ans;
    cin>>t;
    while(t--)
    {
        cin>>n;
        ans=(n-1)*n/2+(n+1)/2;
        cout<<ans*ans<<endl;
    }
    return 0;
}
```
C题 wyh的商机 
待补…
D题  wyh的迷宫 
简单dfs
```cpp
#include<iostream>
using namespace std;
const int N=505;
int px[]={0,0,1,-1},py[]={-1,1,0,0};
int vis[N][N],blag,n,m;
char s[N][N];
void dfs(int x,int y)
{
    if(s[x][y]=='t')
    {
        blag=1;
        return ;
    }
    vis[x][y]=1;
    for(int i=0;i<4;i++)
    {
        int dx=x+px[i];
        int dy=y+py[i];
        if(blag)
          return ;
        if(s[dx][dy]!='x'&&dx>=0&&dx<n&&dy>=0&&dy<m&&vis[dx][dy]==0)
          dfs(dx,dy);
    } 
}
int main()
{
    int t,x,y;
    cin>>t;
    while(t--)
    {
        cin>>n>>m;
        memset(vis,0,sizeof(vis));
        blag=0;
        for(int i=0;i<n;i++)
          for(int j=0;j<m;j++)
          {
              cin>>s[i][j];
              if(s[i][j]=='s')
              {
                  x=i;
                  y=j;
              }
          }
        dfs(x,y);
        if(blag)
          cout<<"YES"<<endl;
        else
          cout<<"NO"<<endl;
    }
    return 0;
}
```
E题 wyh的阶乘 
统计n以内5的倍数中5的个数
```cpp
#include<iostream>
using namespace std;
int main()
{
    int t,n;
    cin>>t;
    while(t--)
    {
        cin>>n;
        int ans=0;
        while(n)
        {
            ans+=n/5;
            n/=5;
        }
        cout<<ans<<endl;
    }
    return 0;
}
```
F题 wyh的集合 
简单数学
```cpp
#include<iostream>
using namespace std;
int main()
{
    long long t,n;
    cin>>t;
    while(t--)
    {
        cin>>n;
        cout<<n*n/4<<endl;
    }
    return 0;
}
```
G题 wyh的考核 
待补…
H题  wyh的吃鸡 
优先队列优化的Dijkstra+bfs
```cpp
#include<iostream>
#include<cstdio>
#include<cstring>
#include<queue>
using namespace std;
typedef long long int ll;
const ll INF=0x7fffffff;
const int N=110;
struct node{
    int x,y,t;
    ll len;
    node(int a,int b,ll c,int d):x(a),y(b),len(c),t(d) {};
    bool operator<(const node &p)const{
        return len>p.len;
    }
};
priority_queue<node> q;
int n,k,sx,sy;
int px[]={1,-1,0,0},py[]={0,0,1,-1};
bool vis[N][N][3];
char map[N][N];
ll bfs()
{
    while(!q.empty())
      q.pop();
    memset(vis,0,sizeof(vis));
    q.push(node(sx,sy,0,2));
    while(!q.empty())
    {
        node u=q.top();
        q.pop();
        if(vis[u.x][u.y][u.t])
          continue;
        vis[u.x][u.y][u.t]=true;
        if(map[u.x][u.y]=='X')
          return u.len;
        for(int i=0;i<4;i++)
        {
            int dx=u.x+px[i];
            int dy=u.y+py[i];
            if(dx>=0&&dy>=0&&dx<n&&dy<n&&!vis[dx][dy][u.t]&&map[dx][dy]!='O')
              q.push(node(dx,dy,u.len+u.t,map[dx][dy]=='C'?1:u.t));
        }
    }
    return INF;
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        cin>>n>>k;
        for(int i=0;i<n;i++)
          for(int j=0;j<n;j++)
          {
              cin>>map[i][j];
              if(map[i][j]=='S')
                sx=i,sy=j;
          }
        ll ans=bfs();
        if(ans<=k)
          printf("YES\n%lld\n",ans);
        else
          printf("NO\n");
    }
    return 0;
}
```
I题   wyh的物品 
二分计算差值最小
```cpp
#include<iostream>
#include<cmath>
#include<algorithm>
#include<cstdio>
using namespace std;
#define INF 0x3f3f3f3f
const int maxn=100005;
long long c[maxn],v[maxn];
double f[maxn];
int n,k;
bool Judge(double x)   //单位价值为 x 
{
    for(int i=0;i<n;i++)   
      f[i]=x*c[i]-v[i];  //如果选择第i个物品，还差多少体积 
    sort(f,f+n);
    double sum=0;
    for(int i=0;i<k;i++)
      sum+=f[i];
    return sum<=1e-5;
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        cin>>n>>k;
        for(int i=0;i<n;i++)
          cin>>c[i]>>v[i];
        double l=0,r=INF,mid;
        while(l<=r)
        {
            mid=(l+r)/2;
            if(fabs(r-l)<1e-3)
              break;
            if(Judge(mid))
              l=mid;
            else
              r=mid;
        } 
        printf("%.2lf\n",mid);
    }
    return 0;
}
```
J题  wyh的问题 
待补…
K题 wyh的数列 
快速幂
```cpp
#include<iostream>
using namespace std;
typedef unsigned long long ULL;
ULL f[5005];
ULL quick_mod(ULL a,ULL b,int mod)
{
    ULL ans=1;
    a=a%mod;
    while(b)
    {
        if(b%2)
          ans=(ans*a)%mod;
        a=(a*a)%mod;
        b/=2;
    }
    return ans;
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        ULL a,b,c;
        cin>>a>>b>>c;
        f[0]=0,f[1]=1;
        int res;
        for(int i=2;;i++)
        {
            f[i]=(f[i-1]+f[i-2])%c;
            if(f[i]==1&&f[i-1]==0)
            {
                res=i-1;
                break;
            }
        }
        cout<<f[quick_mod(a,b,res)]<<endl;
    }
    return 0;
}
```
L题 wyh的天鹅 
权值线段树
```
#include<iostream>
using namespace std;
const int maxn=1e6+5;
int s[4*maxn]; 
void build(int l,int r,int root)
{
    s[root]=0;
    if(l==r)
      return ;
    int mid=(l+r)/2;
    build(l,mid,2*root);
    build(mid+1,r,2*root+1);
}
void update(int pos,int val,int l,int r,int root)
{
    if(l==r)
    {
        s[root]+=val;
        return ;
    }
    int mid=(l+r)/2;
    if(pos<=mid)
      update(pos,val,l,mid,2*root);
    else
      update(pos,val,mid+1,r,2*root+1);
    s[root]=s[2*root]+s[2*root+1];
} 
int query(int k,int l,int r,int root)
{
    if(l==r)
      return l;
    int mid=(l+r)/2;
    if(s[2*root+1]>=k)
      return query(k,mid+1,r,2*root+1);
    else
      return query(k-s[2*root+1],l,mid,2*root);
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,m,x;
        cin>>n>>m;
        build(0,maxn,1);
        for(int i=0;i<n;i++)
        {
            cin>>x;
            update(x,1,0,maxn,1);
        }
        for(int i=0;i<m;i++)
        {
            char s[10];
            cin>>s>>x;
            if(s[0]=='q')
              cout<<query(x,0,maxn,1)<<endl;
            else if(s[0]=='i')
              update(x,1,0,maxn,1);
            else
              update(x,-1,0,maxn,1);
        }
    }
    return 0;
}
```
M题 wyh的数字 
水题
```cpp
#include<iostream>
using namespace std;
char s[105];
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        cin>>s;
        int num=0;
        for(int i=0;s[i]!='\0';i++)
          if(s[i]='7')
            num++;
        cout<<num<<endl;
    }
    return 0;
}
```
